@using RMS.Utility
@model RMS.Web.Models.ViewModel.TenderSpecDeptOpinionViewModel
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@Html.HiddenFor(x => x.FormId)
@Html.HiddenFor(x => x.TenderSpecDeptOpinion.FileNo)
@Html.HiddenFor(x => x.FlowSerial)
<div class="row">
    <div class="panel panel-primary">
        <div class="panel-heading" style="text-align:center;">
            @Model.FormName
        </div>
        <div class="panel-body">
            @*<table>
                <tr>
                    <td>@Html.ActionLink("Add New", "Create", "TenderSpecDeptOpinions", new { @class = "btn btn-info" })</td>
                    <td>
                        @using (Html.BeginForm("SearchByKey", "TenderSpecDeptOpinions", FormMethod.Get))
                        {

                            <table>
                                <tr>
                                    <td>
                                        @Html.TextBoxFor(x => x.SearchKey, new { @class = "textBoxsize form-control", tabindex = 1, autofocus = "autofocus", placeholder = "Name" })
                                    </td>
                                    <td>
                                        <button class="custom-serach-btn"><i class="fa fa-search" aria-hidden="true"></i></button>
                                    </td>
                                </tr>
                            </table>
                        }
                    </td>
                </tr>
            </table>*@
            <div>
                <h4 class="h4 text-info">@Model.Message</h4>
            </div>
            @if (Model.TenderSpecDeptOpinions.Count > 0 )
            {
                 
                <div class="">
                    @{ int rowNo = 0; }
                    <table class="table table-bordered table-hover table-striped">

                        <tr style="background-color: #654FA1;color: white">
                            <td style="text-align: center; font-size: 15px">Serial No</td>
                            <td style="text-align: center; font-size: 15px">Project Name</td>
                            <td style="text-align: center; font-size: 15px">Tender Name</td>
                            @*<td style="text-align: center; font-size: 15px">Issue Date</td>*@
                            <td style="text-align: center; font-size: 15px">Department</td>
                            <td style="text-align: center; font-size: 15px">File PDF</td>
                       

                        </tr>

                        @foreach (var item in Model.TenderSpecDeptOpinions)
                        {
                            <tr>
                                <td style="">@(rowNo += 1)</td>
                                <td style="">@item.Demand.FileNo</td>
                                <td style="">@item.TenderName</td>
                                <td style="">@*@item.IssueDate*@</td>
                                <td style="">@item.Department.Name</td>
                                @if (item.OpinionUrl != null)
                                {

                                    <td style="text-align: center; color: blue">
                                        <a href="@Url.Action("Show", "CommitteeRaiseLetters", new { url = item.OpinionUrl })" style="" target="_blank"><img src="~/Content/icons/32x32/pdf.png" /></a>
                                    </td>
                                }
                                else
                                {
                                    <td style="text-align: center; color: blue">
                                        @*<a href="#" style="" onclick="location.href = '@Url.Action("Show", "Demand", new { url = item.FileUrl})'"><img src="~/Content/icons/32x32/pdf.png" /></a>*@
                                    </td>
                                }
                                
                                <td>
                                    <a href="@Url.Action("TenderSpecificationDelete", "TenderSpecDeptOpinions", new { DemandId = item.TenderSpecOpinionGivenId })" class="btn btn-warning">
                                        <span class="glyphicon glyphicon-remove" aria-hidden="true">Cancel</span>
                                    </a>
                                </td> 
                            </tr>
                        }
                    </table>
                </div>
            }
            else if (Model.StaffRequirementDeptOpinions.Count > 0 )
            {
                <div class="">
                    @{ int rowNo = 0; }
                    <table class="table table-bordered table-hover table-striped">

                        <tr style="background-color: #654FA1;color: white">
                            <td style="text-align: center; font-size: 15px">Serial No</td>
                            <td style="text-align: center; font-size: 15px">Project Name</td>
                            <td style="text-align: center; font-size: 15px">Tender Name</td>
                            <td style="text-align: center; font-size: 15px">@*Issue Date*@</td>
                            <td style="text-align: center; font-size: 15px">Department</td>
                            <td style="text-align: center; font-size: 15px">File PDF</td>
                           

                        </tr>

                        @foreach (var item in Model.StaffRequirementDeptOpinions)
                        {
                            <tr>
                                <td style="">@(rowNo += 1)</td>
                                <td style="">@item.Demand.FileNo</td>
                                <td style="">@item.TenderName</td>
                                <td style="">@*@item.IssueDate*@</td>
                                <td style="">@item.Department.Name</td>
                                @if (item.OpinionUrl != null)
                                {

                                    <td style="text-align: center; color: blue">
                                        <a href="@Url.Action("Show", "CommitteeRaiseLetters", new { url = item.OpinionUrl })" style="" target="_blank"><img src="~/Content/icons/32x32/pdf.png" /></a>
                                    </td>
                                }
                                else
                                {
                                    <td style="text-align: center; color: blue">
                                        @*<a href="#" style="" onclick="location.href = '@Url.Action("Show", "Demand", new { url = item.FileUrl})'"><img src="~/Content/icons/32x32/pdf.png" /></a>*@
                                    </td>
                                }

                            </tr>
                        }
                    </table>
                </div>
            }
            else if (Model.ContractDeptOpinions.Count > 0)
            {
                <div class="">
                    @{ int rowNo = 0; }
                    <table class="table table-bordered table-hover table-striped">

                        <tr style="background-color: #654FA1;color: white">
                            <td style="text-align: center; font-size: 15px">Serial No</td>
                            <td style="text-align: center; font-size: 15px">Project Name</td>
                            <td style="text-align: center; font-size: 15px">Tender Name</td>
                            @*<td style="text-align: center; font-size: 15px">Issue Date</td>*@
                            <td style="text-align: center; font-size: 15px">Department</td>
                            <td style="text-align: center; font-size: 15px">File PDF</td>
                        

                        </tr>

                        @foreach (var item in Model.ContractDeptOpinions)
                        {
                            <tr>
                                <td style="">@(rowNo += 1)</td>
                                <td style="">@item.Demand.FileNo</td>
                                <td style="">@item.TenderName</td>
                                <td style="">@*@item.IssueDate*@</td>
                                <td style="">@item.Department.Name</td>
                                @if (item.OpinionUrl != null)
                                {

                                    <td style="text-align: center; color: blue">
                                        <a href="@Url.Action("Show", "CommitteeRaiseLetters", new { url = item.OpinionUrl })" style="" target="_blank"><img src="~/Content/icons/32x32/pdf.png" /></a>
                                    </td>
                                }
                                else
                                {
                                    <td style="text-align: center; color: blue">
                                        @*<a href="#" style="" onclick="location.href = '@Url.Action("Show", "Demand", new { url = item.FileUrl})'"><img src="~/Content/icons/32x32/pdf.png" /></a>*@
                                    </td>
                                }

                            </tr>
                        }
                    </table>
                </div>
            }
       

        </div>
    </div>
    <a href="@Url.Action("Create", "TenderSpecDeptOpinions", new { fileId = Model.TenderSpecDeptOpinion.FileNo, formId = Model.FormId })" class="btn btn-info">
        Back To Create Page
       
    </a>
</div>
